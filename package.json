{
  "name": "application-nourriture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node --check src/domain.js && node --check src/main.js && npm run test"
  }
}
